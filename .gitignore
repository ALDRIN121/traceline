# Secrets — never committed (public repository, no bundled credentials)
.env
*.env
!*.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/

# Fixture byproducts — a standalone sample-agent run writes result.json into
# its own directory; only the DB copy is authoritative.
fixtures/sample-agent/result.json
build/

# Local state
*.db
*.sqlite
*.sqlite3
.superpowers/
.worktrees/

# Playwright MCP scratch (browser verification artifacts)
.playwright-mcp/
node_modules/
playwright-report/
test-results/
.agents/
.codex/
.commandcode/
