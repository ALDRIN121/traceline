[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-agent-eval"
version = "0.1.0"
description = "Configurable evaluation engine for agentic systems: parse, harness-author, isolate, trace, score, explain."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
dependencies = [
    "pydantic>=2.7",
    "fastapi>=0.111",
    "uvicorn>=0.30",
    "httpx>=0.27",
    "psycopg[binary]>=3.1",
    "python-multipart>=0.0.20",
    "cel-python==0.5.0",
    "litellm>=1.63.0",
    "cryptography>=44.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
]

[project.scripts]
eval-engine = "llm_agent_eval.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.data-files]
"share/llm-agent-eval/migrations" = [
    "migrations/0001_workflow_objects.sql",
    "migrations/0002_jobs_secrets.sql",
    "migrations/0003_sessions.sql",
    "migrations/0003_source_imports.sql",
    "migrations/0004_datasets.sql",
    "migrations/0005_targets.sql",
    "migrations/0006_model_profile_kinds.sql",
]
"share/llm-agent-eval/skills/dashboard-authoring" = [
    "skills/dashboard-authoring/SKILL.md",
    "skills/dashboard-authoring/schema.json",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "browser: requires the browser end-to-end harness (future suite; not yet present)",
    "integration: requires documented disposable external services (future suite; not yet present)",
    "live: hits a real LLM provider; requires DEEPSEEK_API_KEY (run last)",
]
addopts = "-m 'not live'"
