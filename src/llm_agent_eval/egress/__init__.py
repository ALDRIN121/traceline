"""Recording egress foundations (blocker 2): CA, budget, and a recording proxy.

This package holds only foundations. Until the rootless runtime joins each run
to this proxy as the sole route (per-run internal network, no default egress),
nothing here is release evidence.
"""
